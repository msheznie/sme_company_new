# gears_srm_backend

`docker-compose up`

`docker exec -it gears-srm bash`

`composer install`

`cp .env.example .env`

`php artisan key:generate `

`php artisan migrate`

`php artisan migrate:refresh --seed`

`php artisan passport:keys`
